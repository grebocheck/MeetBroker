# Початковий аналіз і формування обсягу

Дата: 2026-07-29

## Запит 001 — аналіз конкурсного ТЗ

> Проведи детальний аналіз поставленої задачі для конкурсу, поки без
> складання плану реалізації, лише аналіз і пропозиції, файл з задачею це
> spec-uk.pdf

### Результат

Проаналізовано обов'язкові функції, часову модель, інтервальні конфлікти,
серверну авторизацію, календарний UX, тести, документацію, оцінювання та
бонуси. Файли проєкту на цьому кроці не змінювалися.

## Запит 002 — початкова інтерпретація

> Скласти короткий план інтерпретації ТЗ, обрати модульний і розширюваний
> підхід, розділити застосунок на кілька контейнерів з окремим Nginx, додати
> якісне адміністрування та можливість обмежувати користувача. Інтерфейс має
> бути простим та інтуїтивним. Розробку почати з дизайн-схеми й кількох
> варіантів асетів. Вести якісну історію комітів і журнал промптів.

### Результат

Запропоновано React, NestJS, PostgreSQL, Nginx і Docker Compose. Виділено
користувацьку й адміністративну частини, дизайн-first підхід, сфокусоване
тестування та правила історії змін.

## Запит 003 — гнучкий доступ і корпоративна перевірка

> Не створювати окрему роль обмеженого користувача. Обмеження мають бути
> гнучкими й, можливо, автоматично тимчасовими. Потрібна ознака
> підтвердженого користувача та можливість адміністратора верифікувати його,
> щоб стороння людина не отримала доступ до корпоративної системи.

### Результат

Розділено автентифікацію, підтвердження email, корпоративне схвалення, роль і
capability-based обмеження. Тимчасові заборони мають необов'язкову дату
завершення і припиняють діяти автоматично. Несхвалені користувачі не бачать
внутрішній календар та імена співробітників.

## Запит 004 — профілі, аватари та старт документації

> Додати фотографію профілю, опис і зручне відображення профілю. Якщо
> користувач не хоче справжню фотографію, він має обрати один із готових
> варіантів. Почати складати план, створити журнал і коміти.

### Результат

Створено робочий план і окремий запис архітектурного рішення. Профіль містить
ім'я, короткий опис і вибір між готовим та завантаженим аватаром. Медіа
проходять серверну перевірку, а сховище приховане за абстракцією.

### Пов'язані файли

- `spec-uk.pdf`;
- `docs/PROJECT_PLAN.md`;
- `docs/decisions/0001-product-access-and-profiles.md`;
- `docs/prompt-journal/README.md`;
- `docs/prompt-journal/2026-07-29-initial-scope.md`.

### Пов'язані коміти

- `cb53050` — додано вихідне конкурсне ТЗ;
- `46aa3eb` — зафіксовано продуктовий та архітектурний напрям;
- `1e02eae` — створено журнал промптів і навігацію документації.

## Запит 005 — початок дизайн-етапу

> Чудово, виконуй.

Контекстом запиту є погоджений наступний крок: сформувати карту екранів,
кілька візуальних напрямів і варіанти готових аватарів.

### Результат

Створено:

- карту публічних, користувацьких та адміністративних екранів;
- три напрями календарного інтерфейсу: Kyiv Light, Orbit і Dobra;
- порівняння напрямів та рекомендований комбінований підхід;
- стилістичний аркуш із 12 готовими аватарами;
- маніфест збережених assets і нормалізованих промптів.

Зображення згенеровані вбудованим `imagegen` і перенесені до репозиторію.
Вони використовуються як референси для вибору дизайн-системи, а не як
готові екрани чи фінальні production-assets.

### Пов'язаний коміт

- `75d7a68` — додано календарні концепти й аркуш аватарів.

## Запит 006 — події, сповіщення, доступність і вибір дизайну

> Додати сповіщення про запрошення через Telegram або email із простим
> підключенням бота; окрему сторінку відкритих заходів із можливістю
> приєднатися або відмовитися; керовані години роботи, прибирання й технічні
> блокування кімнат з адміністративним винятком. За дизайн взяти третій
> варіант із меню першого, передбачити брендування компанії, модульну i18n та
> світлу/темну тему.

### Результат

- додано модель запрошених учасників і відкритих подій;
- визначено внутрішні, email і Telegram-сповіщення;
- підключення Telegram спроєктовано через одноразовий deep link до бота;
- для надійної доставки обрано transactional outbox і окремий worker;
- робочі години відділено від одноразових і повторюваних `RoomBlock`;
- адміністративний override потребує причини й аудиту;
- Dobra затверджено як основу з лівим меню Kyiv Light;
- i18n, теми й брендування закладено через стабільні контракти та
  семантичні design tokens.

### Пов'язаний коміт

- `550adda` — розширено модель бронювання, доступність кімнат і план
  сповіщень.

## Запит 009 — розширювані канали сповіщень

> Побудувати сповіщення на абстрактному класі, щоб способи доставки можна
> було розширювати, а користувач обирав групи сповіщень і канали для кожної
> групи.

### Результат

- уточнено план і архітектурне рішення;
- визначено матрицю `INVITATIONS | CHANGES | REMINDERS | ACCESS` ×
  `IN_APP | EMAIL | TELEGRAM`;
- зовнішні канали відокремлено контрактом `NotificationChannel` і registry;
- email та Telegram оформлено незалежними адаптерами;
- профіль користувача переводиться на керування матрицею замість пов'язаних
  глобальних перемикачів.

## Запит 010 — керований DB layer

> Використати готові модулі для безпечнішої роботи з базою та аналог Alembic
> для JavaScript; прийнято рекомендацію переходити поступово.

### Рішення

- `node-pg-migrate` обрано для міграцій PostgreSQL;
- Drizzle ORM обрано для поступового переведення runtime-запитів;
- наявна SQL-історія зберігається й baseline-иться автоматично;
- demo seed відокремлюється від production migration step;
- перехід виконується невеликими незалежними комітами з перевіркою кожного
  етапу.

## Запит 011 — наближення робочого UI до погоджених макетів

> Продовжити розробку, зосередившись на тому, що поточний інтерфейс ще не
> відтворює погоджений дизайн: Dobra як основа та меню Kyiv Light.

### Результат

- календар переведено на теплу світлу палітру, типографічну ієрархію та
  щільність Dobra;
- desktop-навігацію уточнено за структурою Kyiv Light, а на вузькому екрані
  збережено компактну нижню навігацію;
- додано власний ботанічний знак, виразний блок активної кімнати, лінію
  поточного часу та легенду типів подій;
- матрицю каналів сповіщень зроблено компактною на desktop і перетворено на
  підписані блоки без горизонтальної прокрутки на mobile;
- адаптивний вигляд перевірено реальним браузерним рендером при `1440×1000`
  і `390×844`;
- smoke-сценарій, типи, тести, production-збірка та аудит залежностей пройшли
  успішно.

### Пов'язаний коміт

- `9fee59f` — робочий календар і оболонку наближено до затвердженого
  візуального напряму.
- `cc51a6f` — виправлено адаптивність матриці налаштувань сповіщень.

## Запит 012 — виразніша дизайн-система з натхненням Persona 3 Reload

> Поточний дизайн усе ще виглядає слабко; покращити композицію і шрифти,
> використавши Persona 3 Reload як можливе джерело натхнення.

### Рішення

- від референсу взято не конкретні екрани чи assets, а принципи: образ
  занурення, сині багатошарові площини, м'який рух, геометричну типографіку
  та швидко помітні активні стани;
- візуальну метафору MeetBroker сформульовано як «потік робочого дня»;
- `Manrope Variable` використано для основного тексту, а
  `Unbounded Variable` — для великих заголовків і коротких акцентів;
- обидва шрифти постачаються локально з підтримкою кирилиці, без залежності
  від зовнішнього CDN;
- світлу і темну теми переведено на спільну синьо-блакитну палітру з рожевим
  акцентом поточного часу;
- навігація, календар, вхід, події, профіль і адміністрування перевірені в
  браузері; прямі персонажі, логотипи та графіка гри не використовуються.

### Пов'язаний коміт

- `ebe6ea4` — додано нову синю дизайн-систему та локальні variable fonts.

## Запит 013 — глобальні перемикачі теми й мови

> Додати видимі перемикачі світлої/темної теми та мови, не ховаючи їх лише
> в налаштуваннях профілю.

### Результат

- у бокову панель додано швидкі перемикачі теми та `UA/EN`;
- на мобільному екрані вони переходять у компактний блок над контентом;
- зміни зберігаються через профільний API та переживають перезавантаження;
- значення в повній формі профілю синхронізуються зі швидкими контролями;
- обидва напрями перемикання перевірено через API з відновленням початкових
  налаштувань після тесту.

### Пов'язаний коміт

- `bdc0428` — додано постійні глобальні перемикачі теми та мови.

## Запит 014 — логотип, аватари, оптимізація фото та вибір мови

> Оновити логотип під новий стиль; виправити завеликий контур навколо малих
> preset-аватарів; замість вимоги фото до 2 МБ автоматично стискати його;
> показувати меню вибору мови й перезавантажувати сторінку після вибору.

### Результат

- ботанічний знак замінено на похилу `M` із лінією потоку та сигнальною
  крапкою, узгоджену з синьою дизайн-системою;
- preset-аватари масштабуються до контейнера, а активне кільце стало
  компактним;
- сервер приймає фото до 12 МБ як захисну межу, перевіряє декодоване
  зображення, автоматично орієнтує, кадрує до `512×512` і зберігає у WebP;
- тестове PNG розміром 2 410 428 байтів успішно оптимізовано до WebP
  розміром 27 610 байтів, після чого тестовий avatar було прибрано;
- перемикач мови відкриває меню `Українська / English`, зберігає вибір і
  перезавантажує сторінку лише після успішної відповіді API.

### Пов'язані коміти

- `9707d30` — розширено й перевірено pipeline оптимізації avatar uploads;
- `ea27b07` — оновлено знак, picker аватарів і меню вибору мови.

## Запит 015 — searchable timezone selector і нові аватари

> Створити UI-селектор із пошуком і використати його для часового поясу;
> прибрати дублікати налаштувань мови й теми з профілю; перегенерувати
> аватари відповідно до нової дизайн-системи.

### Результат

- створено повторно використовуваний `SearchSelect` з ARIA-combobox,
  фільтрацією, вибором мишею, клавішами стрілок, `Enter` та `Escape`;
- часовий пояс обирається з нативного IANA-переліку
  `Intl.supportedValuesOf("timeZone")` із fallback-набором;
- мову й тему прибрано з форми профілю, оскільки ними керують глобальні
  sidebar controls;
- вбудованим `imagegen` перегенеровано production sprite sheet 4×3:
  дванадцять різних професійних персонажів, cobalt/cyan/navy палітра,
  діагональні форми, flow-лінії та рожеві signal accents;
- точний контракт `1448×1086` і стабільний порядок `avatar-01`…
  `avatar-12` збережено.

### Пов'язані коміти

- `abb965a` — додано searchable selector і спрощено профіль;
- `6f4f39f` — перегенеровано production avatars і оновлено asset manifest.

## Запит 016 — заміна кругових фонових мотивів

> Круглий градієнт із тонкими кільцями вибивається з дизайну; замінити його
> доречнішою декорацією.

### Результат

- концентричні кільця в основній робочій області замінено на асиметричні
  діагональні шари й легкі потокові лінії;
- кругові контури бокової панелі також замінено на кутові напівпрозорі
  площини, щоб декоративна мова залишалась послідовною;
- декорації використовують наявні theme tokens, не перехоплюють взаємодію
  та коректно працюють у світлій і темній темах;
- результат перевірено у браузері, а застосунок пройшов smoke-перевірку.

### Пов'язаний коміт

- `0fdbb6a` — замінено кільцеві мотиви на потокову геометрію.

## Запит 017 — адаптивний профіль і виразні поверхні

> Виправити верстку профілю, додати заливку карткам сповіщень та іншим
> подібним станам, стилізувати смуги прокрутки й замінити затісний badge
> поточної дати.

### Результат

- пропорції профільної сітки та колонки матриці сповіщень зроблено
  адаптивними, а її вміст більше не виходить за межі картки;
- picker аватарів сам визначає кількість колонок і не обрізає останній
  елемент на проміжних ширинах;
- settings, admin, calendar та empty-state поверхні отримали суцільну
  тематичну заливку, яка відділяє їх від декоративного фону;
- додано узгоджені тонкі scrollbar-стилі для Firefox і Chromium/WebKit;
- залитий badge сьогоднішньої дати замінено на рожево-блакитний нижній
  акцент із достатнім простором навколо тексту;
- профіль, сповіщення, календар і мобільну матрицю перевірено у браузері,
  застосунок пройшов typecheck, production build і smoke-перевірку.

### Пов'язаний коміт

- `77ae15e` — виправлено профільні картки, прокрутку та календарні акценти.

## Запит 018 — фото кімнат і критичне падіння booking dialog

> Керування кімнатами зосередити в адмініструванні, дозволити додавати фото
> та зробити стильову заглушку. Виправити зникнення інтерфейсу після кліку
> на вільну клітинку календаря.

### Результат

- встановлено точну причину падіння: controller повертав невирішений
  `Promise` у полі `users`, через що booking dialog викликав `.filter()` не
  на масиві й React демонтував увесь root;
- controller тепер очікує результат сервісу, а dialog додатково захищений
  від некоректної відповіді та дозволяє створити бронювання без запрошень,
  якщо список колег тимчасово недоступний;
- додано міграцію `image_path`, захищені admin endpoints для завантаження,
  заміни й видалення фото кімнати;
- зображення перевіряється через Sharp, кадрується до `1600×900`,
  оптимізується у WebP, а попередній файл видаляється;
- створено спільний `RoomVisual`: реальне фото або синя діагональна
  заглушка з монограмою та рожевим сигналом використовуються в календарі
  й адміністративному списку;
- додавання кімнати разом із фото, заміна та видалення наявного фото
  доступні у вкладці `Адміністрування → Кімнати`;
- smoke перевіряє resolved-масив колег і повний lifecycle фото
  upload → API response → delete → placeholder; browser test підтвердив
  відкриття booking modal без помилок у console.

### Пов'язані коміти

- `f7ad0bb` — усунено падіння форми бронювання на завантаженні колег;
- `941dc47` — додано адміністративне керування зображеннями кімнат.

## Запит 019 — редагування бронювань зі сповіщеннями

> Дозволити редагувати заплановані бронювання; після редагування можуть
> надсилатися сповіщення.

### Результат

- організатор може відкрити власне бронювання в календарі, натиснути
  `Редагувати` й змінити назву, час, тип події та склад учасників;
- серверний `PATCH /api/bookings/:id` виконується транзакційно та повторно
  перевіряє власника, майбутній час, 30-хвилинну сітку, тривалість, робочі
  години, технічні блокування, місткість і конфлікти кімнати;
- no-op редагування не створює зайвих записів і сповіщень;
- наявні учасники отримують локалізоване `BOOKING_UPDATED` у категорії
  `CHANGES`, видалені — повідомлення про зміну участі, а нові — звичайне
  `BOOKING_INVITATION`;
- доставка використовує чинний outbox і абстрактні notification channels,
  тому враховує вибрані користувачем групи та способи доставки;
- smoke перевіряє create → update → `BOOKING_UPDATED` → cancel, а browser
  test — відкриття передзаповненої edit-форми без console errors.

### Пов'язані коміти

- `a1707ec` — додано транзакційне редагування та update notifications;
- `d1f925f` — додано organizer edit flow у календарі.

## Запит 020 — демо-набір фотографій переговорних

> Згенерувати набір фотографій для демо-переговорних, але одну кімнату
> залишити без фото, щоб можна було побачити дизайнерську заглушку.

### Imagegen prompt set

Спільна основа для п’яти окремих built-in `imagegen` викликів:

> Wide 16:9 meeting-room thumbnail for a corporate booking application;
> premium architectural interior photography with a restrained stylized
> editorial finish; cobalt, deep navy and cyan palette with one small
> coral-pink signal accent; readable at thumbnail size; no people, text,
> letters, logos or watermark.

Відмінності сцен:

- `Гагарін` — шість місць, овальний стіл, орбітальна геометрія та кругове
  cyan-світло;
- `Акваріум` — вісім місць, скляні перегородки й делікатні водяні
  відблиски без буквального акваріума;
- `Дніпро` — дванадцять місць, панорамні вікна та широка потокова лінія
  стельового освітлення;
- `Марс` — десять місць, темні грановані панелі та стримане тепле сигнальне
  підсвічування без зображень планети;
- `Софія` — компактна кімната на чотири місця, круглий стіл, дерево й
  м'яке денне світло.

### Результат

- кожен asset згенеровано окремим built-in `imagegen` викликом;
- результати нормалізовано до `1600×900 WebP` із quality `82`; увесь набір
  займає близько 640 КБ;
- міграція прив'язує static URLs до п'яти seed-кімнат, а `Обрій` навмисно
  залишається без фото;
- upload адміністратора має пріоритет над demo URL, а видалення фото
  повертає кімнату до дизайнерської заглушки;
- smoke виконує lifecycle фото саме на fallback-кімнаті, не змінюючи
  постійний демо-набір;
- календар і вкладку `Адміністрування → Кімнати` перевірено у браузері.

### Пов'язаний коміт

- `695a0af` — додано оптимізований демо-набір і migration mapping.

## Запит 021 — методологія нахилів і sidebar-анімація

> Узгодити кнопку скасування з дизайном, формалізувати нахил як частину
> методології, збільшити відступ тексту в боковому меню та прибрати
> прямокутний кадр під час анімації пунктів.

### Результат

- додано спільні shape tokens `--shape-action` і `--shape-nav`, щоб форма
  компонента не дублювалася локальними `polygon`;
- основна й небезпечна дії тепер мають один спрямований силует, а їхня
  семантика відрізняється палітрою;
- внутрішній відступ пунктів sidebar збільшено до 20 px зліва і 24 px
  справа;
- nav-силует застосовується вже в базовому стані, тому hover змінює лише
  заливку, колір і зміщення без проміжного прямокутного кадру;
- у браузері перевірено drawer бронювання, обидві теми, computed styles
  активних і неактивних пунктів та відсутність console errors;
- web typecheck і production build завершилися успішно.

### Пов'язаний коміт

- `f03ff15` — уніфіковано похилу геометрію дій і навігації.

## Запит 022 — підтвердження скасування та пошук учасників

> Замінити браузерне підтвердження скасування події на дизайнерське
> модальне вікно з деталями, а селектор учасників підготувати до великої
> кількості користувачів і додати пошук.

### Результат

- створено спільний `CancelBookingDialog` для календаря та сторінки
  «Мої бронювання» замість двох викликів `window.confirm`;
- модальне вікно показує назву, кімнату, повну дату, часовий інтервал і,
  коли доречно, кількість учасників;
- діалог має семантику `alertdialog`, безпечний початковий фокус на
  збереженні бронювання, блокує повторні дії під час запиту та показує
  серверну помилку без закриття;
- селектор учасників винесено в модульний `ParticipantPicker` із пошуком за
  ім'ям, вибраними користувачами у chips, швидким видаленням, порожнім
  станом і контролем місткості кімнати;
- browser test підтвердив фільтрацію списку, відсутність native confirm,
  деталі модалки, коректний фокус і відсутність console errors;
- web typecheck, production build, Vitest і повний smoke-прогін завершилися
  успішно.

### Пов'язаний коміт

- `b0507dc` — додано модальне скасування та пошуковий вибір учасників.

## Запит 023 — продовження реалізації за конкурсною специфікацією

> Продовжити виконання плану відповідно до `spec-uk.pdf`, зосередившись на
> наступних незавершених вимогах.

### Результат

- сторінку «Мої бронювання» переведено на посторінковий API-контракт:
  сервер повертає до 30 записів, `hasMore` і точний `nextOffset`;
- для минулих і майбутніх бронювань доступне поступове підвантаження без
  втрати вже показаних рядків, окремо обробляється помилка наступної
  сторінки;
- у календар додано серверний фільтр мінімальної місткості кімнати;
- після зміни фільтра автоматично вибирається перша придатна кімната, а
  бронювання недоступне, доки кімнату не завантажено;
- додано явні стани помилки списку кімнат і відсутності кімнат потрібної
  місткості замість нескінченного skeleton;
- стабільні API-коди бронювання розподіляються до відповідних частин форми:
  назва, часовий інтервал, учасники або загальна помилка;
- локальні помилки порожньої назви й некоректного інтервалу показуються біля
  полів, а сервер залишається джерелом правил сітки, тривалості, майбутнього
  часу, робочих годин, конфліктів і місткості;
- browser test підтвердив, що фільтр від 10 місць повертає лише кімнати на
  10, 12 і 16 місць, коректно перемикає розклад і не створює console errors;
- API-контракт пагінації перевірено через Nginx, а typecheck, production
  build і повний smoke-прогін завершилися успішно.

### Пов'язані коміти

- `309657c` — реалізовано пагінацію історії бронювань;
- `6bb753c` — підключено фільтрацію кімнат за місткістю;
- `6684b64` — помилки бронювання розміщено біля відповідних полів.

## Запит 024 — виразніша ієрархія скасування

> Зробити кнопку скасування помітнішою в «Моїх бронюваннях», нахилити
> безпечну кнопку в модалці, замінити слабкий градієнт червоним акцентом і
> зробити danger-кнопку повністю залитою з білим текстом.

### Результат

- дія скасування в рядку майбутнього бронювання використовує повноцінний
  danger-стиль замість малопомітної ghost-кнопки;
- додано семантичні tokens `danger-fill`, `danger-fill-hover` і
  `danger-contrast` для контрольованого контрасту в обох темах;
- danger-кнопка отримала суцільну червону заливку, білий текст і виразний
  hover без відмови від спільної похилої геометрії;
- «Залишити бронювання» використовує `button--slanted`, зберігаючи
  нейтральну семантику secondary-дії;
- модалка має червону верхню лінію та бічний акцент картки деталей замість
  слабкого рожевого градієнта;
- браузерна перевірка підтвердила світлу й темну теми, контраст, однакові
  `clip-path`, безпечний початковий фокус і відсутність console errors.

### Пов'язаний коміт

- `f7dccd2` — посилено візуальну ієрархію скасування бронювання.

## Запит 025 — повна рамка похилої secondary-кнопки

> Виправити відсутню рамку по боках кнопки «Залишити бронювання» у
> спокійному стані.

### Результат

- стандартний прямокутний `border`, який обрізався через `clip-path`,
  замінено двошаровою похилою формою;
- зовнішній шар відповідає за безперервний контур, внутрішній — за поверхню
  кнопки та її hover-стан;
- рамка тепер повторює верхню, нижню й обидві похилі грані;
- browser test підтвердив вигляд, фокус, hover-сумісну структуру й відсутність
  console errors; повний smoke-прогін успішний.

### Пов'язаний коміт

- `006221f` — відновлено повний контур похилої secondary-кнопки.

## Запит 026 — адміністративне керування бронюваннями

> Продовжити роботу над адміністративним блоком і надати профілю Ігоря
> Коваля рівень адміністратора.

### Результат

- до адміністрування додано окрему секцію бронювань із фільтрами майбутніх,
  минулих, скасованих і всіх записів;
- серверний пошук працює за назвою зустрічі, кімнатою, ім'ям або email
  організатора і повертає повні дані кімнати, організатора та учасників;
- модальне вікно показує дату, час, формат, учасників, override і дані
  попереднього скасування;
- адміністратор може примусово скасувати чуже майбутнє бронювання лише після
  введення змістовної причини;
- причина зберігається в аудиті та надсилається організатору й учасникам
  через наявний transactional outbox;
- smoke-сценарій розширено перевіркою каталогу й повноти адміністративних
  деталей бронювання;
- окремий наскрізний API-сценарій підтвердив примусове скасування, аудит і
  сповіщення організатора;
- browser test підтвердив список і модальне вікно у світлій темі без
  критичних помилок;
- профіль `user@meetbroker.local` (Ігор Коваль) підвищено до `ADMIN` через
  захищений API, тому зміна ролі присутня в журналі аудиту;
- typecheck, Vitest, production build і повний smoke-прогін завершилися
  успішно.

### Пов'язаний коміт

- `90dc04e` — додано адміністративний каталог і примусове скасування
  бронювань.

## Запит 027 — журнал подій та атрибуція адміністратора

> Додати до адміністрування журнал основних подій, дозволити адміністратору
> скасовувати або змінювати події з явним відображенням адміністративного
> втручання, а також виключити `codebase-memory` з GitHub.

### Результат

- вкладку «Аудит» розширено до пошукового журналу подій із категоріями
  бронювань, доступу та кімнат;
- журнал показує час, дію, об'єкт, виконавця, email, адміністративний статус
  і розкривні деталі на кшталт причини, інтервалу та змін складу учасників;
- до журналу потрапляють створення, звичайне й адміністративне редагування,
  скасування, відповіді на запрошення, приєднання та вихід із відкритих
  подій;
- у каталозі бронювань додано адміністративне редагування через спільний
  `BookingDialog`, щоб не дублювати правила форми;
- зміна чужого бронювання вимагає причину, фіксується як
  `BOOKING_UPDATED_BY_ADMIN` і надсилає організатору та учасникам
  повідомлення з ім'ям адміністратора;
- негативний smoke-сценарій підтверджує, що чуже бронювання не можна змінити
  без адміністративної причини;
- browser test перевірив журнал із реальними рядками, фільтри та модальне
  вікно адміністративної зміни без критичних помилок;
- `.codebase-memory/` додано до `.gitignore`, а раніше відстежувані
  артефакти прибрано з Git-індексу без видалення локального графа;
- typecheck, Vitest, production build і розширений smoke-прогін завершилися
  успішно.

### Пов'язані коміти

- `ec98dc4` — додано журнал подій та атрибутоване адміністративне
  редагування;
- `fa00cae` — припинено відстеження локальних артефактів codebase-memory.

## Запит 028 — робочі години та повторювана недоступність кімнат

> Повністю переосмислити недоступність кімнати: підтримати разову проблему,
> події кожні кілька днів або тижнів і робочі години кімнати; усі ці правила
> мають відображатися на панелі розкладу.

### Результат

- робочі години, фактичний інтервал недоступності та правило повторення
  розділено на три незалежні поняття;
- додано міграцію `room_block_series`; повторення підтримує кожні `N` днів
  або вибрані дні кожного `N`-го тижня з обов'язковою кінцевою датою;
- входження серії матеріалізуються з урахуванням `Europe/Kyiv`, тому наявна
  перевірка конфліктів бронювання працює без окремого паралельного механізму;
- адміністратор бачить активні разові винятки й серії, може скасувати їх,
  а також редагувати години роботи біля кожної кімнати;
- календар окремо показує разову/повторювану недоступність і затінює
  ненатискні слоти поза робочими годинами;
- створення та скасування правил входить до категорії кімнат у журналі
  подій;
- smoke-сценарій перевіряє зміну й відновлення робочих годин, чотири
  входження серії, їх появу в розкладі, скасування та аудит;
- typecheck, Vitest, production build, міграція в Docker і повний smoke
  завершилися успішно.

### Пов'язані коміти

- `67d04ce` — додано модель і API повторюваної недоступності;
- `24b0d72` — додано адміністративний UI та календарне відображення.

## Запит 029 — канонічні компоненти, картки кімнат і demo-приклади

> Прибрати візуальну відмінність кнопки «Змінити подію», канонізувати
> UI-компоненти, виправити розкладку керування кімнатами й показати приклади
> використання недоступності.

### Результат

- додано канонічний React-компонент `Button` із семантичними варіантами
  `primary`, `secondary`, `danger` і `ghost`, контрольованим розміром та
  формою;
- адміністративні дії й модальне скасування переведено з ручних комбінацій
  CSS-класів на спільний компонент;
- «Закрити» та «Змінити подію» тепер мають однакову secondary-геометрію, а
  скасування зберігає окрему danger-семантику;
- правила використання базових компонентів і похилої геометрії зафіксовано
  в `docs/design/UI_COMPONENTS.md`;
- рядки переговорних перетворено на самостійні картки: ідентичність,
  робочі години та робота з фото більше не стискаються в один рядок;
- часові поля розширено, тому локалізовані значення з `AM/PM` не втрачають
  частину часу;
- demo-міграція додає разову «Перевірку проєктора» для кімнати «Марс» і
  серію «Ранкове прибирання» щопонеділка та щосереди для «Акваріума»;
- browser check при `1260×950` підтвердив обидва приклади, спільний клас
  кнопок, відсутність переповнення основного контенту та console errors;
- typecheck, Vitest, Docker migration, production build і повний smoke
  завершилися успішно.

### Пов'язані коміти

- `03d072b` — додано канонічні кнопки та стабільні картки кімнат;
- `3b120d2` — додано два демонстраційні правила недоступності.

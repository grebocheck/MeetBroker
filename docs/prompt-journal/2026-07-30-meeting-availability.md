# Зайнятість учасників, онлайн-зустрічі та особистий календар

## Запит 052 — конфлікти розкладу учасників

> Якщо користувач уже прив'язаний до зустрічі на певний час, його не можна
> додати до іншої зустрічі на цей самий час. Автор має бачити причину і всі
> зустрічі, з якими виникла накладка.

### Результат

- організатор, запрошені та підтверджені учасники блокують свій часовий
  інтервал; відхилене запрошення більше не вважається зайнятістю;
- перевірка діє під час створення, редагування, прийняття запрошення та
  приєднання до відкритої події;
- повторювані зустрічі перевіряються для кожного входження;
- advisory locks користувачів беруться в стабільному порядку, щоб паралельні
  запити не створювали накладок;
- API повертає `ATTENDEE_BUSY` зі згрупованими іменами, назвами, часом та
  роллю в конфліктних зустрічах, а форма показує ці дані автору.

### Пов'язані коміти

- `b95fe2c` — додано конкурентно безпечну перевірку зайнятості учасників.

## Запит 053 — онлайн-зустрічі та календар «Мої зустрічі»

> Додати онлайн-зустрічі з обов'язковим посиланням і окремий календар усіх
> зустрічей, де користувач є організатором або запрошеним.

### Результат

- міграція додає тип `ROOM | ONLINE`, nullable `room_id` і HTTPS-посилання
  з database constraint для взаємовиключних форматів;
- правила тривалості, 30-хвилинного кроку, минулого часу та зайнятості людей
  спільні, але онлайн-зустріч не залежить від робочих годин і блокувань
  переговорної;
- створення й редагування підтримують онлайн-посилання, а списки, відкриті
  події та адмінка коректно працюють без кімнати;
- окремий шестиденний адаптивний календар показує роль користувача, формат,
  деталі, учасників і безпечну кнопку переходу до онлайн-зустрічі;
- посилання відкритої онлайн-події повертається лише організатору або
  користувачу, який уже приєднався;
- міграцію застосовано в demo Docker stack; наскрізно перевірено створення
  онлайн-зустрічі, відповідь `409 ATTENDEE_BUSY`, персональний календар і
  скасування тестової події.

### Пов'язані коміти

- `c3ecb5b` — додано online domain, міграцію, правила та API календаря;
- `5a6e41d` — додано форму онлайн-зустрічі й персональний календар.

## Запит 054 — favicon застосунку

> У вкладці браузера немає favicon.

### Результат

- створено легкий SVG favicon на основі геометрії чинного `BrandMark`;
- використано темно-синє поле, похилі сині площини, бірюзову лінію та
  рожевий сигнальний акцент дизайн-системи;
- favicon явно підключено в HTML, а browser theme color узгоджено з фоном
  бренду.

### Пов'язані коміти

- функціональний коміт цього запису.

## Запит 055 — редизайн «Моїх зустрічей» і часова вісь

> Сторінка «Мої зустрічі» потребує сильнішого дизайнерського опрацювання, а
> значення часової осі основного розкладу візуально з'їжджають від ліній.

### Результат

- шестиколонкову сторінку з великими порожніми площинами замінено на
  персональний agenda-екран;
- верхній рівень показує наступну зустріч, кількість зустрічей сьогодні та
  запрошень, що очікують відповіді;
- шестиденний план став вертикальним таймлайном із виразною датою,
  часовим блоком, форматом, організатором, роллю та статусом відповіді;
- похила графіка, акценти й hover-поведінка використовують чинні tokens і
  геометрію MeetBroker;
- на вузьких екранах таймлайн переходить у дворядкові компактні картки без
  горизонтальної прокрутки;
- підписи часу основного розкладу тепер центруються відносно точних меж
  32-піксельних слотів через transform замість крихкого ручного `-7px`;
  перший підпис має окреме правило, тому більше не обрізається заголовком.

### Пов'язані коміти

- функціональний коміт цього запису.

## Запит 056 — сценічна підкладка персонального agenda

> Новий екран уже виглядає цікаво, але таймлайну бракує власної підкладки;
> варто ще розвинути композицію з натхненням від Persona 3 Reload.

### Результат

- сторінку розширено для великих екранів, щоб композиція не закінчувалася
  посеред доступного полотна;
- шестиденний agenda отримав окрему темно-синю кутову сцену з діагональними
  світловими площинами, тонкою cyan/pink крайкою та службовим маркером;
- світлий інформаційний шар залишено поверх сцени для контрасту й
  доступності, а рядки таймлайна мають дуже легке спрямоване тонування;
- картка наступної зустрічі отримала велику напівпрозору фонову типографіку,
  яка підсилює асиметричну композицію;
- для мобільного екрана форма підкладки, відступи та декоративні площини
  спрощуються окремими правилами;
- використано лише власні tokens, геометрію і бренд MeetBroker без
  копіювання конкретних ресурсів іншої гри.

### Пов'язані коміти

- функціональний коміт цього запису.

## Запит 057 — обкладинки зустрічей і полірування форм

> Заголовок «Повторення» врізається в рамку картки. Потрібно дозволити
> прикріплювати до зустрічі картинку для фону в розкладі та показу у
> відкритих подіях, а також прибрати ледь видимі випадкові цифри з
> персонального agenda.

### Результат

- `legend` повторення замінено внутрішнім заголовком і допоміжним підписом,
  тому текст більше не перетинає рамку;
- додано міграцію `011_booking_images` і захищені organizer/admin endpoints
  завантаження, заміни та видалення обкладинки;
- Sharp перевіряє вміст, враховує EXIF rotation, обрізає зображення до
  1400×788 та стискає у WebP; вхідний ліміт — 12 МБ;
- файли зберігаються в persistent uploads volume, старі версії фізично
  видаляються після успішної заміни;
- форма показує preview, дозволяє замінити або прибрати обкладинку і не
  створює дубль бронювання, якщо саме завантаження файлу потребує повтору;
- обкладинка використовується як затемнений фон блоку в кімнатному
  розкладі, як верхнє медіа відкритої події та як візуальна площина
  наступної зустрічі;
- технічний напис `06 / DAYS` замінено зрозумілим декоративним маркером, а
  велику випадкову напівпрозору площину agenda замінено тонким патерном;
- міграцію застосовано у Docker; наскрізно перевірено upload, Open Events
  response, видалення файлу й скасування тестової зустрічі.

### Пов'язані коміти

- `43af207` — додано схему, обробку файлів і API обкладинок зустрічі;
- `3b0da7a` — додано picker і використання обкладинок в інтерфейсі.

## Запит 058 — широкий editorial-layout робочого простору

> Візуальна мова «Моїх зустрічей» вийшла вдалою, тому її варто поширити на
> інші сторінки, активніше використовувати всю площу екрана й виправити
> обрізаний напис `Personal / Agenda`.

### Результат

- «Мої бронювання», «Відкриті події», «Сповіщення», «Профіль» та
  «Адміністрування» більше не обмежені центральним `narrow-page`;
- додано спільний editorial-layout із великим фоновим маркером сторінки,
  похилою світловою площиною, акцентною рискою заголовка та короткою
  анімацією появи;
- бронювання й сповіщення на широких екранах використовують адаптивні
  двоколонкові сітки, відкриті події — ширшу карткову сітку, а профіль —
  повноцінну асиметричну двоколонкову композицію;
- картки отримали спільні кутові акценти й легку глибину без зміни
  семантики або канонічних UI-компонентів;
- при ширині до 1050 px профіль знову складається в одну колонку, а режим
  reduced motion фактично вимикає нову анімацію;
- праву межу службового підпису agenda відсунуто від діагонального зрізу,
  тому `Personal / Agenda` більше не обрізається до `Personal / A`.

### Пов'язані коміти

- `d72b95a` — поширено широкий editorial-layout на основний робочий простір.

## Запит 059 — геометрія і типографіка бокової навігації

> Активний пункт бокового меню має завершуватися скошеним краєм замість
> гострої стрілки, використовувати чорний текст та стилістично відповідніший
> шрифт.

### Результат

- стрілоподібний кінець і внутрішню виїмку замінено паралельними
  діагональними зрізами, узгодженими з формою основних кнопок;
- desktop-варіант як і раніше виходить за ліву межу sidebar, тому активна
  площина візуально починається поза екраном;
- активний і hover-стани використовують майже чорний текст та іконку
  замість синіх;
- навігаційні підписи переведено на компактний `Unbounded` із окремою
  вагою активного стану;
- геометрія лишається придатною для згорнутого sidebar, де показуються лише
  іконки.

### Пов'язані коміти

- `4193082` — оновлено форму, колір і типографіку пунктів навігації.

## Запит 060 — сценічний редизайн розкладу кімнати

> Над екраном «Розклад» варто додатково попрацювати як дизайнер,
> продовжуючи енергійну асиметричну стилістику з натхненням від
> Persona 3 Reload.

### Результат

- екран переосмислено як командний live-план кімнати, відмінний від
  персонального agenda у «Моїх зустрічах»;
- верхній блок отримав збільшене фото кімнати, сильнішу типографічну
  ієрархію, великий фоновий напис `Schedule` та окрему темну панель
  фільтрів і головної дії;
- календар поміщено у власну темно-синю кутову сцену з діагональними
  лініями, службовим підписом і cyan/pink крайкою;
- сама часова сітка залишилася світлою та точною, але отримала виразніші
  заголовки днів, стан сьогодні, часову колонку, поточну лінію та нижню
  легенду;
- картки подій використовують канонічні асиметричні радіуси й легку
  реакцію на наведення;
- для мобільного viewport сцена спрощується, фото й заголовок
  зменшуються, а календар зберігає дві видимі колонки без горизонтальної
  прокрутки;
- композицію вручну перевірено в Chromium на 1600×1000 і 390×844.

### Пов'язані коміти

- `cd331fd` — перероблено композицію та оформлення розкладу кімнати.

## Запит 061 — баланс робочого простору після редизайну

> «Мої зустрічі» мають бути першими в меню; фонову типографіку треба
> підняти, бронювання й сповіщення повернути до одного контрольованого
> стовпчика, а контрастну панель фільтрів розкладу перенести всередину
> контенту.

### Результат

- «Мої зустрічі» переміщено на першу позицію робочої навігації, «Розклад»
  тепер іде другим; мобільна п'ятиелементна навігація зберігає той самий
  порядок;
- великі editorial-маркери сторінок, `Schedule` і `NEXT` піднято вище,
  щоб основні картки та керування не перекривали їх;
- списки «Мої бронювання» і «Сповіщення» знову одноколонкові, обмежені
  шириною 1120 px і мають тонку вертикальну направляючу в дусі
  персонального agenda;
- елементи списків займають повну ширину цього потоку, але не розтягуються
  на все робоче полотно;
- фільтри місткості, кімнати й головна дія перенесені з hero-заголовка у
  світлу верхню смугу самої календарної картки;
- селектори тепер використовують приглушені поверхні й звичайний контраст
  форми замість великої темно-синьої панелі;
- desktop-композицію перевірено у Chromium на сторінках розкладу,
  бронювань і сповіщень.

### Пов'язані коміти

- `f4ba196` — перебалансовано навігацію, списки та керування розкладом.

## Запит 062 — нижній баланс полотна і дії сповіщень

> На широких екранах нижній лівий кут робочого полотна часто порожній;
> його варто підтримати графікою. Дії в заголовку сповіщень розташовані
> надто далеко від основного тексту.

### Результат

- до глобального робочого фону додано окрему нижню ліву кутову площину з
  тонкими діагоналями та слабким синьо-бірюзовим тонуванням;
- новий шар має менший контраст за верхню композицію і не перехоплює
  події, тому лишається суто декоративним;
- площина прив'язана до межі основного контенту після desktop sidebar і
  показується лише на широких екранах понад 1200 px;
- шапка сповіщень тепер має ту саму максимальну ширину 1120 px, що й
  список;
- блок «Прочитати всі / Налаштувати канали» більше не притискається до
  правого краю viewport, а йде одразу після текстового блоку заголовка;
- результат візуально перевірено у Chromium на 1600×1000.

### Пов'язані коміти

- `728e261` — додано нижню фонову площину та зближено дії сповіщень.

## Запит 063 — фільтри у шапці таблиці розкладу

> Фільтри місткості й кімнати краще інтегрувати в header календарної
> таблиці — поруч із часовим поясом і навігацією між днями.

### Результат

- навігацію періоду, фільтри, вибір кімнати, головну дію та часові пояси
  об'єднано в одну візуальну панель усередині календарної сцени;
- окрему смугу фільтрів прибрано, тому таблиця починається компактніше й
  елементи керування сприймаються як єдиний toolbar;
- на широкому екрані всі групи займають один рядок;
- до 1400 px панель переходить у два рядки, залишаючись єдиним header,
  а на мобільному екрані складається в одну колонку;
- адаптивна композиція не створює горизонтальної прокрутки календаря;
- результат перевірено у Chromium на 1600×1000 і 1280×900.

### Пов'язані коміти

- `2a75e3c` — інтегровано фільтри та головну дію в header календаря.

## Запит 064 — правий баланс фону та прямі календарні панелі

> Додати графічний елемент на порожній правий край полотна та прибрати
> заокруглення внутрішніх країв таблиці розкладу.

### Результат

- на правому нижньому краї desktop-полотна з'явилася окрема напівпрозора
  площина з тонкими діагоналями, яка врівноважує верхню й ліву графіку;
- акцент розташований за контентом, не перехоплює події та вимикається на
  екранах до 1200 px;
- внутрішні світлі панелі календарів «Розклад» і «Мої зустрічі» отримали
  прямі кути;
- зовнішні темно-сині сцени зберегли фірмовий скошений силует;
- результат перевірено у Chromium на 1600×1000 для обох календарів.

### Пов'язані коміти

- `00258d8` — додано правий фоновий акцент і випрямлено календарні панелі.

## Запит 065 — редизайн адміністративного workspace

> Адміністративне меню досі виглядає центрованою службовою формою; його
> потрібно пройти як дизайнер і узгодити з візуальною мовою застосунку.

### Результат

- адміністративна сторінка більше не обмежується центрованою шириною
  1440 px і використовує все доступне робоче полотно;
- hero отримав сильнішу editorial-типографіку, службовий статус і
  виразнішу фонову композицію;
- чотири розділи оформлено як нумеровану темно-синю control-панель з
  канонічним скошеним активним станом;
- контент розміщено у власній кутовій сцені з підписом поточного розділу,
  діагональною графікою та cyan/pink крайкою;
- таблиці користувачів, бронювань і журналу використовують повноширинну
  структуру, спільний toolbar та чіткі hover-маркери;
- керування кімнатами перетворено на широкий workspace: каталог,
  вибрана кімната, доступність і винятки формують одну композицію;
- форма створення кімнати більше не центрується всередині робочої області;
- навігація адаптується до двох колонок на мобільному екрані, довгі
  підписи переносяться, а декоративні елементи не стискають контент;
- перевірено вкладки користувачів, кімнат і журналу в Chromium на
  1600×1000, 1280×900 та 390×844.

### Пов'язані коміти

- `27b2ba3` — повністю перебудовано композицію адміністративного розділу.

## Запит 066 — уніфікація фонових editorial-маркерів

> В адмініструванні фонове слово має бути `CONTROL`, на сторінці подій —
> `EVENTS`; виведення великих фонових слів потрібно вирівняти між
> сторінками.

### Результат

- маркер адміністрування скорочено з `CONTROL / ADMIN` до `CONTROL`;
- маркер відкритих подій скорочено з `OPEN / EVENTS` до `EVENTS`;
- усі editorial-маркери тепер використовують спільну область між
  горизонтальними padding сторінки;
- короткі й довгі слова мають однакову верхню базову лінію, праву межу,
  масштаб, прозорість і обрізання;
- окремий збільшений і зміщений варіант для адміністративної сторінки
  прибрано;
- візуально порівняно `CONTROL`, `EVENTS` і `BOOKINGS` на 1600×900.

### Пов'язані коміти

- `548008c` — уніфіковано значення й геометрію фонових маркерів.

## Запит 067 — Persona-inspired система модалок

> Опрацювати модальні вікна як окрему дизайн-систему з натхненням від
> Persona 3 Reload.

### Результат

- backdrop став повноекранною темно-синьою сценою, яка розмиває весь
  застосунок разом із боковою панеллю;
- додано великі діагональні площини, але збережено достатній контраст
  навколо самої модалки;
- модалки отримали спільну асиметричну геометрію, сервісну cyan/pink
  смугу, нижній світловий акцент і глибшу тінь;
- header і footer відокремлено як самостійні композиційні зони;
- фоновий editorial-маркер змінюється за контекстом: `RESERVE`,
  `DETAILS`, `ACCESS`, `LINK`, `CONTROL` або `ALERT`;
- секції налаштувань доступу та картка наслідків скасування приведено до
  тієї самої скошеної геометрії;
- небезпечна дія зберігає виразний червоний сценарій, не змішуючись із
  основними синіми діями;
- drawer деталей отримав споріднений силует, діагональну графіку та
  окрему cyan-кромку;
- для мобільних екранів модалка переходить у зручний bottom-sheet, а для
  `prefers-reduced-motion` вимикаються нові появи й переміщення;
- візуально перевірено створення бронювання та його скасування на
  1600×1000, а сценарій скасування також на 390×844.

### Перевірки

- `npm run typecheck --workspace @meetbroker/web`;
- `npm test --workspace @meetbroker/web` — 8 тестів;
- `npm run build --workspace @meetbroker/web`;
- `git diff --check`;
- production-збірку розгорнуто через Docker Compose.

### Пов'язані коміти

- `ebea544` — створено спільну cinematic-систему модалок і drawer.

## Запит 068 — канонізація header, admin tabs і blur модалок

> Замінити фоновий маркер сповіщень на `LOG`, узгодити header-и сторінок,
> спростити композицію адміністрування та повернути модалкам попередній
> розмитий фон.

### Результат

- фоновий editorial-маркер сторінки сповіщень змінено з
  `SIGNAL / LOG` на `LOG`;
- header сповіщень більше не обмежений шириною списку, тому нижня лінія
  доходить до правої межі робочого полотна;
- editorial-сторінки, розклад і «Мої зустрічі» використовують спільну
  висоту, нижній padding, вирівнювання та геометрію акцентної лінії;
- в розкладі текстовий блок вирівняно по нижній базовій лінії незалежно
  від висоти зображення кімнати;
- в адмініструванні прибрано зовнішню темно-синю stage-рамку, службовий
  caption і зайві декоративні площини навколо робочого контенту;
- адміністративні вкладки перероблено як єдиний набір світлих
  канонічних сегментів із однаковими станами hover та active;
- мобільні правила вкладок і admin workspace узгоджено з новою
  геометрією;
- backdrop модалок і drawer повернуто до попереднього м'якого blur без
  додаткового темно-синього фону та великих діагональних площин;
- візуально перевірено сповіщення, розклад, адміністрування та створення
  бронювання на 1600×1000.

### Перевірки

- `npm run typecheck --workspace @meetbroker/web`;
- `npm test --workspace @meetbroker/web` — 8 тестів;
- `npm run build --workspace @meetbroker/web`;
- `git diff --check`;
- оновлену production-збірку розгорнуто через Docker Compose.

### Пов'язані коміти

- `b64f8ed` — уніфіковано header-и, admin tabs і overlay blur.

## Запит 069 — компактні адміністративні стрічки та широка кімнатна сцена

> Вирівняти фонові слова розкладу й адміністрування з іншими сторінками;
> не розтягувати користувачів, бронювання та журнал на всю ширину, але
> залишити повноширинне керування кімнатами й додатково його допрацювати.

### Результат

- локальний маркер `SCHEDULE` опущено на спільну верхню базову лінію з
  editorial-маркерами інших сторінок;
- admin hero повернуто до канонічного розміру заголовка, тому `CONTROL`
  має ту саму композицію й не конкурує з назвою сторінки;
- активний admin workspace тепер отримує семантичний модифікатор
  `users`, `bookings`, `rooms` або `audit`;
- користувачі, бронювання та журнал обмежені шириною 1120 px, як стрічка
  на сторінці «Мої бронювання»;
- зовнішню картку цих трьох розділів прибрано: toolbar став окремою
  панеллю, а записи — самостійними скошеними картками;
- до списків додано тонку вертикальну навігаційну лінію, компактні
  проміжки, hover-зсув і м'яку глибину;
- пагінацію вирівняно з початком карток, а мобільні правила не
  повертають стару рамку й негативні відступи;
- кімнати зберегли повну ширину: каталог отримав стабільну колонку
  270–340 px, а робоча область — більший hero, канонічні радіуси,
  виразнішу панель доступності та чіткішу секцію винятків;
- візуально перевірено користувачів, бронювання, кімнати, журнал і
  розклад на 1600×1000.

### Перевірки

- `npm run typecheck --workspace @meetbroker/web`;
- `npm test --workspace @meetbroker/web` — 8 тестів;
- `npm run build --workspace @meetbroker/web`;
- `git diff --check`;
- production-збірку повторно розгорнуто через Docker Compose.

### Пов'язані коміти

- `12c9fe0` — перебудовано адміністративні списки й кімнатний workspace.

## Запит 070 — точна метрика фонового слова розкладу

> Довести header розкладу до повної відповідності іншим сторінкам:
> акцентна лінія має перетинати нижню частину фонового слова, а не лише
> торкатися її.

### Результат

- `SCHEDULE` переведено з окремої приблизної метрики на ті самі
  `font-size`, `line-height`, вагу та letter-spacing, що й глобальні
  editorial-маркери;
- абсолютну позицію перераховано відносно локального header так, щоб
  фактична координата на сторінці відповідала глобальним 42 px;
- акцентна лінія тепер перетинає нижню частину гліфів так само, як для
  `LOG`, `CONTROL`, `EVENTS` та інших сторінок;
- мобільне перевизначення також приведено до тієї самої метрики замість
  окремого зменшеного масштабу;
- результат візуально перевірено у Chromium на 1600×1000.

### Перевірки

- `npm run build --workspace @meetbroker/web`;
- `git diff --check`;
- production-збірку розгорнуто через Docker Compose.

### Пов'язані коміти

- `2202272` — синхронізовано типографічну метрику маркера розкладу.

## Запит 071 — відновлення стилів полів керування доступом

> У модалці керування доступом поля вводу відображаються без стилів.

### Результат

- встановлено причину: поля `UserAccessDialog` не використовували
  канонічний markup-контракт `.field`;
- select функції та області дії, обидва datetime-поля й textarea причини
  переведено на спільний компонентний патерн `.field`;
- текстові підписи винесено в окремі `<span>`, тому вони отримали
  узгоджену вагу, розмір і відступ;
- danger-textarea повного відкликання доступу також обгорнуто у `.field`
  з доступним прихованим підписом;
- відновлено спільні заливки, рамки, padding, focus-ring і поведінку
  світлої та темної тем;
- діалог візуально перевірено у Chromium на 1600×1000.

### Перевірки

- `npm run typecheck --workspace @meetbroker/web`;
- `npm test --workspace @meetbroker/web` — 8 тестів;
- `npm run build --workspace @meetbroker/web`;
- `git diff --check`;
- production-збірку розгорнуто через Docker Compose.

### Пов'язані коміти

- `94ca05c` — відновлено канонічне оформлення полів керування доступом.

## Запит 072 — актуальні IANA-ідентифікатори часових поясів

> У селекторі немає `Europe/Kyiv`, але пропонується застарілий
> `Europe/Kiev`; використати актуальні значення.

### Результат

- встановлено джерело проблеми: `Intl.supportedValuesOf("timeZone")`
  залежить від версії ICU браузера й може повертати застарілі aliases;
- додано нормалізаційний шар для актуальних IANA-назв, зокрема
  `Europe/Kiev` → `Europe/Kyiv`;
- також оновлюються відомі aliases для Kolkata, Kathmandu, Yangon, Nuuk,
  Ho Chi Minh City, Faroe, Pohnpei, Chuuk та Asmara;
- список об'єднується з гарантованими популярними зонами, дедуплікується
  й сортується;
- уже збережене старе значення профілю відображається як актуальне, але
  сумісність із наявними даними зберігається;
- у браузері перевірено 419 опцій: `Europe/Kyiv` присутній,
  `Europe/Kiev` відсутній.

### Перевірки

- `npm run typecheck --workspace @meetbroker/web`;
- `npm test --workspace @meetbroker/web` — 8 тестів;
- `npm run build --workspace @meetbroker/web`;
- `git diff --check`;
- production-збірку розгорнуто через Docker Compose.

### Пов'язані коміти

- `cf4cbaa` — додано нормалізацію актуальних IANA-ідентифікаторів.

## Запит 073 — CLI-контур адміністраторів, розбан і тест Telegram

> Заборонити адміністраторам через застосунок позбавляти доступу інших
> адміністраторів або видавати роль адміністратора; такі дії виконувати
> лише окремо через CLI. Додати відновлення доступу та перевірити нове
> сповіщення у вже прив'язаному Telegram.

### Результат

- HTTP-маршрут зміни ролі видалено, а керування роллю адміністратора
  перенесено до серверного CLI з командами `promote`, `demote`, `revoke`
  і `restore`;
- CLI працює лише за наявності `DATABASE_URL`, блокує зміну останнього
  активного адміністратора, завершує сесії при відкликанні доступу та
  записує операторські дії в аудит із джерелом `CLI`;
- через вебінтерфейс та API заборонено відкликати або відновлювати доступ
  адміністратора, створювати для нього обмеження чи відкликати наявні;
- схвалення користувача більше не скидає відкликання доступу, тому його
  не можна використати як неявний розбан;
- для звичайних користувачів додано явну дію «Відновити доступ» із
  записом `USER_ACCESS_RESTORED` у журналі подій;
- у профілі підключеного Telegram додано дію «Надіслати тест», яка
  використовує production outbox і worker, примусово обираючи лише
  Telegram без email;
- одне тестове повідомлення пройшло реальний пайплайн із першої спроби:
  API повернув `202`, а outbox завершився статусом `SENT` без помилки;
- README доповнено операторськими командами та гарантіями CLI.

### Перевірки

- `npm run typecheck`;
- `npm test` — 29 тестів;
- `npm run build`;
- `git diff --check`;
- живий API: відкликання доступу іншого адміністратора повертає
  `403 ADMIN_MANAGED_BY_CLI`, видалений role-route — `404`, відновлення
  звичайного користувача — `204`;
- production-збірку API, worker і web розгорнуто через Docker Compose;
- Telegram outbox перевірено до кінцевого статусу `SENT`.

### Пов'язані коміти

- `0e9f7c1` — додано захищений CLI-контур адміністраторів, системний
  розбан і тестову Telegram-доставку.

## Запит 074 — HOME-header, перенесення онлайн-зустрічі та іконки каналів

> Зробити шапку «Моїх зустрічей» повноширинною з фоновим словом `HOME`,
> перенести кнопку створення онлайн-зустрічі до «Моїх бронювань» і
> додати значки Email та Telegram у профілі.

### Результат

- «Мої зустрічі» переведено на спільний `editorial-page`-контракт із
  повною шириною та фоновим маркером `HOME`;
- прибрано окреме обмеження сторінки до 1580 px, а marker, декоративний
  фон і акцентна лінія тепер використовують канонічну геометрію інших
  вкладок;
- дію «+ Онлайн-зустріч» забрано з персонального календаря й додано до
  header-а «Моїх бронювань»;
- після створення онлайн-зустрічі синхронно інвалідуються історія
  бронювань, персональний календар та відкриті події;
- систему SVG-іконок доповнено `MailIcon` і `TelegramIcon`;
- обидва канали в профілі отримали впізнавані скошені icon-блоки,
  узгоджені з основною дизайн-системою;
- три екрани візуально перевірено у Chromium на 1600×1000.

### Перевірки

- `npm run typecheck --workspace @meetbroker/web`;
- `npm test --workspace @meetbroker/web` — 8 тестів;
- `npm run build --workspace @meetbroker/web`;
- `git diff --check`;
- production web-збірку розгорнуто через Docker Compose.

### Пов'язані коміти

- `71ecb28` — оновлено HOME-header, розміщення онлайн-дії та візуальну
  ідентичність каналів.

## Запит 075 — ширина HOME-контенту та альтернативна навігація

> Лишити повноширинним header «Моїх зустрічей», але повернути контенту
> попередню ширину; зробити фонові слова `HOME` і `NEXT` виразнішими в
> обох темах та покращити навігацію через Tab і, за можливості, геймпад.

### Результат

- header «Моїх зустрічей» лишився повноширинним, а overview та agenda
  знову обмежені максимальною шириною 1580 px і вирівняні ліворуч;
- написи editorial-header більше не будуються як напівпрозорий колір:
  вони змішуються з непрозорою поверхнею та мають окрему підвищену
  контрастність у світлій і темній темах;
- `NEXT` також отримав непрозорий theme-aware колір і більше не губиться
  в патерні картки;
- глобальний focus-ring поширено на кнопки, посилання, поля, селектори,
  textarea, summary й елементи з `tabindex`;
- додано доступне посилання «Перейти до основного вмісту» та focusable
  `main`-контейнер;
- `ModalLayer` централізовано керує початковим фокусом, утримує `Tab`
  всередині діалогу, обробляє `Escape` і повертає фокус ініціатору;
- усі модалки та drawer-и переведено на спільний `onDismiss`-контракт;
- глобальний адаптер геймпада підтримує D-pad/лівий стік для просторового
  вибору, `A` для активації й `B` як `Escape`, а поточний елемент
  підсвічується окремим акцентним кільцем;
- просторовий алгоритм обмежує навігацію активною модалкою, пропускає
  приховані та вимкнені елементи й прокручує новий фокус у видиму область.

### Перевірки

- `npm run typecheck --workspace @meetbroker/web`;
- `npm test --workspace @meetbroker/web` — 8 тестів;
- `npm run build --workspace @meetbroker/web`;
- `git diff --check`;
- Chromium 2400×1200: header — 2018 px, основний контент — 1580 px;
- світлу й темну теми перевірено візуально;
- у живій модалці перевірено початковий фокус, Tab-loop, Escape та
  повернення фокуса на кнопку відкриття;
- синтетичним standard gamepad перевірено D-pad, gamepad focus-mode та
  активацію skip-link кнопкою `A`;
- production web-збірку розгорнуто через Docker Compose.

### Пов'язані коміти

- `f4f480a` — додано системну клавіатурну й геймпадну навігацію та
  відкалібровано HOME/NEXT-композицію.

## Запит 076 — правильний контраст фонових слів

> Виправити інверсне зафарбовування фонових слів: у світлій темі слово
> має бути темнішим, у темній — світлішим; синхронізувати з цим розклад.

### Результат

- додано спільні непрозорі токени `--page-mark-color` і
  `--card-mark-color`;
- у світлій темі колір маркерів тепер походить від темного кольору
  тексту, а в темній — від світлого;
- `HOME`, `CONTROL`, `EVENTS`, `LOG` та інші editorial-маркери
  використовують єдиний токен;
- окремий маркер `SCHEDULE` у розкладі під'єднано до того самого
  канонічного кольору;
- `NEXT` узгоджено з цією логікою відносно поверхні своєї картки.

### Перевірки

- `npm run typecheck --workspace @meetbroker/web`;
- `npm run build --workspace @meetbroker/web`;
- `git diff --check`;
- у Chromium перевірено фактичні computed colors для `HOME`, `NEXT` і
  `SCHEDULE` у світлій та темній темах;
- production web-збірку повторно розгорнуто через Docker Compose.

### Пов'язані коміти

- `accee22` — виправлено напрям контрасту й уніфіковано фонові слова.

## Запит 077 — сині маркери та первинний NEXT

> Фонові слова мають бути синіми, а не сірими; `NEXT` потрібно повернути
> до вигляду з першої версії.

### Результат

- канонічний колір великих фонових слів знову походить від `--primary`;
- для світлої та темної тем підібрано окрему інтенсивність синього без
  домішування сірого кольору тексту;
- `SCHEDULE` лишився синхронізованим з іншими сторінковими маркерами;
- `NEXT` повернено до початкової формули
  `color-mix(in srgb, var(--primary) 11%, transparent)`.

### Перевірки

- `npm run typecheck --workspace @meetbroker/web`;
- `npm run build --workspace @meetbroker/web`;
- `git diff --check`;
- у Chromium звірено computed colors `HOME`, `SCHEDULE` і `NEXT` в обох
  темах;
- production web-збірку повторно розгорнуто через Docker Compose.

### Пов'язані коміти

- `76df6c8` — відновлено синю палітру маркерів і первинний `NEXT`.

## Запит 078 — яскравіші слова та непрозорий NEXT

> У світлій темі зробити фонові слова яскравішими, у темній — світлішими,
> а `NEXT` зробити непрозорим.

### Результат

- частку акцентного синього у сторінкових маркерах збільшено до 34% у
  світлій темі та до 52% у темній;
- `NEXT` зберіг м'яку початкову інтенсивність, але тепер змішується з
  кольором картки замість `transparent`;
- для карткового маркера додано окремий непрозорий
  `--card-mark-color`.

### Перевірки

- `npm run typecheck --workspace @meetbroker/web`;
- `npm run build --workspace @meetbroker/web`;
- `git diff --check`;
- у Chromium перевірено обидві теми та відсутність alpha-каналу в
  computed color `NEXT`;
- production web-збірку повторно розгорнуто через Docker Compose.

### Пов'язані коміти

- `bed9641` — підсилено сині маркери та зроблено `NEXT` непрозорим.

## Запит 079 — focus меню та прозорі сторінкові слова

> Пункти бокового меню мають виділятися рамкою під час навігації через
> Tab; у світлій темі повернути початковий стиль фонових слів, а в темній
> зробити їх напівпрозорими.

### Результат

- для `.nav-item:focus-visible` додано внутрішню cyan-рамку, яку не
  обрізає скошена форма пункту;
- геймпадний focus отримав аналогічну внутрішню accent-рамку;
- активний пункт зберігає світлу заливку і при клавіатурному фокусі;
- у світлій темі сторінкові слова повернено до початкової формули
  `color-mix(in srgb, var(--primary) 7%, transparent)`;
- у темній темі використано окремий напівпрозорий синій маркер з
  інтенсивністю 28%;
- непрозорий вигляд `NEXT` збережено.

### Перевірки

- `npm run typecheck --workspace @meetbroker/web`;
- `npm run build --workspace @meetbroker/web`;
- `git diff --check`;
- у Chromium реальною послідовністю Tab підтверджено `:focus-visible` і
  внутрішню рамку 3 px;
- у Chromium перевірено computed colors маркерів в обох темах;
- production web-збірку повторно розгорнуто через Docker Compose.

### Пов'язані коміти

- `b3b9249` — виправлено focus бокового меню та палітру сторінкових
  маркерів.

## Запит 080 — просторова навігація стрілками

> Додати можливість керувати меню стрілками та переходити ними між
> полями вводу.

### Результат

- клавіші `↑`, `↓`, `←`, `→` під'єднано до наявного просторового
  алгоритму геймпадної навігації;
- у боковому меню стрілки переводять фокус до сусіднього пункту;
- в однорядкових текстових полях `↑` і `↓` переходять між контролами;
- `←` і `→` лишаються для редагування тексту та переходять до сусіднього
  контролу лише на відповідній межі рядка;
- нативну поведінку стрілок збережено для `select`, `textarea`, дат,
  часу, чисел, range, radio та contenteditable;
- пошукові комбобокси не перехоплюються глобально: стрілки продовжують
  відкривати їхній список і змінювати активний варіант;
- просторовий пошук, як і раніше, обмежується активною модалкою та
  прокручує новий focus у видиму область.

### Перевірки

- `npm run typecheck --workspace @meetbroker/web`;
- `npm test --workspace @meetbroker/web` — 8 тестів;
- `npm run build --workspace @meetbroker/web`;
- `git diff --check`;
- у Chromium перевірено перехід `Мої зустрічі → Розклад` клавішею `↓`;
- у профілі перевірено перехід з input до textarea клавішею `↓`;
- окремо підтверджено, що `ArrowDown` у `SearchSelect` лишає focus у
  combobox і відкриває listbox;
- production web-збірку повторно розгорнуто через Docker Compose.

### Пов'язані коміти

- `655c6f0` — додано клавіатурну просторову навігацію стрілками.

## Запит 081 — повний i18n і чотири нові мови

> Перекласти всі тексти застосунку та додати німецьку, іспанську,
> французьку й японську мови.

### Результат

- сформовано повні типізовані каталоги `uk`, `en`, `de`, `es`, `fr` і
  `ja` з однаковими 659 ключами;
- локалізовано звичайний UI, декоративні фонові слова, stage-підписи,
  дати, час і тексти публічних сторінок;
- вибір мови став доступним до входу, а браузерна локаль визначається
  за повним або базовим language tag із fallback на англійську;
- вибрана мова зберігається в профілі, локальному сховищі й
  застосовується після перезавантаження;
- реєстрація передає вибрану локаль, щоб перший лист уже надходив
  правильною мовою;
- API-контракт, DTO та Drizzle-схему розширено всіма шістьма мовами;
- локалізовано email-підтвердження, Telegram-тест і підтвердження
  підключення, запрошення, зміни, скасування, адміністративні дії,
  нагадування та попередження про наступний зайнятий слот;
- для японської додано відповідний системний шрифтовий стек;
- README оновлено актуальним переліком мов.

### Перевірки

- перевірено однаковість усіх ключів і interpolation placeholders у
  шести каталогах;
- `npm run typecheck --workspace @meetbroker/web`;
- `npm run typecheck --workspace @meetbroker/api`;
- `npm test --workspace @meetbroker/web` — 10 тестів;
- `npm test --workspace @meetbroker/api` — 23 тести;
- production-збірки web та API;
- у headless Chromium перевірено `de`, `es`, `fr`, `ja`, кількість
  варіантів у селекторі та відсутність горизонтального overflow;
- контейнерний стек перебудовано через Docker Compose.

### Пов'язані коміти

- `051362b` — додано повні каталоги, мовний UX і локалізацію облікового
  запису;
- `83530f0` — локалізовано повідомлення для всіх каналів доставки.

## Запит 082 — стабільні стрілки та працездатне перемикання мов

> Покращити нестабільну просторову навігацію стрілками та виправити
> перемикання мови, яке не реагувало на вибір.

### Результат

- знайдено причину 500 при зміні мови: старий PostgreSQL
  `users_locale_check` дозволяв лише `uk` та `en`;
- додано міграцію `012_expand_user_locales.sql` для всіх шести мов;
- просторовий алгоритм тепер оцінює межі та вирівнювання елементів, а
  не лише відстань між їхніми центрами;
- локальні combobox та інші компоненти обробляють стрілки раніше за
  глобальну навігацію;
- focus усередині відкритого меню, listbox, grid або tablist не
  перестрибує до сторонніх елементів сторінки;
- невидимі, `aria-hidden`, inert і недоступні для pointer елементи
  вилучено з кандидатів;
- додано unit-тести напрямку, рядків і колонок просторової навігації.

### Перевірки

- міграція `012_expand_user_locales.sql` застосована в Docker;
- через реальний API послідовно збережено `de`, `es`, `fr`, `ja` і
  повернуто `uk`, усі відповіді — HTTP 200;
- `npm run typecheck` та `npm test` для web і API;
- web — 13 тестів, API — 23 тести;
- у Chromium перевірено клік перемикання `uk → de → uk`;
- стрілками перевірено бічне меню, меню мов та перехід
  `Email → Password`;
- японський login перевірено без горизонтального overflow;
- production-стек повторно перебудовано.

### Пов'язані коміти

- `2901302` — розширено DB constraint і стабілізовано просторову
  навігацію.

## Запит 083 — детальне виправлення керування стрілками

> Стрілки в sidebar досі працювали нестабільно: з `UA` не можна було
> перейти вліво до теми та вгору до пунктів основного меню.

### Результат

- знайдено приховані елементи закритого `<details>`, які браузер
  розміщував геометрично, але не дозволяв сфокусувати;
- нащадки закритого `<details>` вилучено з просторових кандидатів;
- виправлено помилку, через яку звичайні елементи помилково отримували
  циклічну поведінку `menu`;
- sidebar лишено м'якою зоною: стрілки стабільні всередині нього, але
  `→` може перевести focus до головного контенту;
- для селектора мови додано повну клавіатурну семантику:
  `↓` відкриває список, `↑/↓` обходять мови циклічно, `←` і `Esc`
  закривають список та повертають focus на summary;
- нативна поведінка стрілок у `select`, датах, часі та інших складних
  полях збережена;
- додано regression-тест, який не дозволяє застосовувати menu-навігацію
  до елемента без composite role.

### Перевірки

- `npm run typecheck --workspace @meetbroker/web`;
- `npm test --workspace @meetbroker/web` — 14 тестів;
- production web-збірку розгорнуто через Docker Compose;
- у Chromium на viewport `1440 × 900` перевірено переходи
  `UA ← Тема`, `UA ↑ Адміністрування`, `Тема → UA`;
- перевірено відкриття списку мов через `↓`, циклічний обхід,
  закриття через `←` та `Esc`;
- перевірено вихід `→` з sidebar до контролу основного контенту.

### Пов'язані коміти

- `615c19e` — виправлено області, приховані кандидати та семантику
  composite-контролів.
